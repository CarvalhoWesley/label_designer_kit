name: label_renderer
description: >
  Contrato LabelRenderer/RendererOptions implementado por cada backend de
  saída (label_renderer_canvas, label_renderer_pdf, label_renderer_argox,
  ...). Pacote Dart puro — nenhuma implementação concreta aqui.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  label_core:
    path: ../label_core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
